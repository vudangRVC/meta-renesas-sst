#@TYPE: Machine
#@NAME: RZ-CMN-yocto machine
#@DESCRIPTION: Reference machine configuration for Renesas RZ Common boards

SOC_FAMILY = "r9a07g"
require conf/machine/include/arm/armv8-2a/tune-cortexa55.inc

# Arm Base System Architecture says v8.0+ is allowed, but FEAT_CRC32 is required
DEFAULTTUNE = "cortexa55"

MACHINE_FEATURES = "acpi alsa bluetooth keyboard pci qemu-usermode rtc screen usbhost wifi opengles opencl hwh264dec hwh264enc"
# PowerVR GSX userspace GPU feature flag (consumed by recipes that key off
# MACHINE_FEATURES, e.g. mesa's PACKAGECONFIG removal on rz-cmn).
MACHINE_FEATURES:append = " gsx"

KERNEL_IMAGETYPE = "Image"
PREFERRED_PROVIDER_virtual/kernel ?= "linux-yocto"
PREFERRED_VERSION_linux-yocto = "6.18.20%"

# Pin OpenCV to 4.9 (libopencv_*.so.409) for prebuilt app compatibility;
# recipe maintained by the meta-rz-opencva layer
PREFERRED_VERSION_opencv = "4.9.0"

# Keep the single rz-cmn rootfs safe for non-V4H boards by making Mesa the
# build-time owner of the generic EGL/GLES/GBM SONAMEs. The V4H PowerVR stack
# is packaged under /usr/lib/pvr and selected at runtime only on Sparrow-Hawk.
PREFERRED_PROVIDER_virtual/egl = "mesa"
PREFERRED_PROVIDER_virtual/libgles2 = "mesa"
PREFERRED_PROVIDER_virtual/libgles3 = "mesa"
PREFERRED_PROVIDER_virtual/libgl = "mesa"
PREFERRED_PROVIDER_virtual/libgbm = "mesa"
PREFERRED_PROVIDER_virtual/mesa = "mesa"

# Install all the kernel modules into the rootfs
MACHINE_EXTRA_RRECOMMENDS += "kernel-modules"
# Install selected pieces of firmware
MACHINE_EXTRA_RRECOMMENDS += "linux-firmware-wl12xx linux-firmware-wl18xx linux-firmware-rtl-nic"

# Use an initramfs and populate it with the kernel modules and key firmware
#INITRAMFS_IMAGE ?= "core-image-initramfs-boot"
#PACKAGE_INSTALL:append:pn-core-image-initramfs-boot = " ${MACHINE_EXTRA_RRECOMMENDS}"

IMAGE_FSTYPES ?= "wic wic.gz wic.bmap"
WKS_FILE ?= "genericarm64.wks.in"

EFI_PROVIDER ?= "${@bb.utils.contains("DISTRO_FEATURES", "systemd", "systemd-boot", "grub-efi", d)}"

# Try to bring up one physical serial console, or a virtualized serial console
SERIAL_CONSOLES ?= "115200;ttySC0 115200;hvc0"

# Support board with PMIC. Default:yes.
# With this option enabled, build both bin files for PMIC board and Concrete board.
PMIC_SUPPORT ?= "1"

# BSP stack
EXTRA_IMAGEDEPENDS += " flash-writer trusted-firmware-a u-boot"


#=====================================QEMU SUPPORT==================================================================================

# runqemu configuration to run a genericarm64 image inside a qemu-system-aarch64. You will need
# to build u-boot explicitly.
IMAGE_CLASSES += "qemuboot"
QB_SYSTEM_NAME = "qemu-system-aarch64"
# Boot the virtual machine with either an emulated Cortex-A76, or the host if using KVM
QB_MACHINE = "-machine virt"
QB_CPU = "-cpu cortex-a76"
QB_CPU_KVM = "-cpu host -machine gic-version=3"
QB_SMP = "-smp 4"
# Boot into U-Boot and let that scan the disk for the next step, don't pass any kernel or filesystem hints
QB_DEFAULT_BIOS = "u-boot.bin"
QB_DEFAULT_KERNEL = "none"
QB_DEFAULT_FSTYPE = "wic"
QB_FSINFO = "wic:no-kernel-in-fs"
# Mount the wic rootfs as a virtio block device
QB_ROOTFS_OPT = "-drive id=root,file=@ROOTFS@,if=none,format=raw -device virtio-blk-pci,drive=root"
# Virtio graphics
QB_GRAPHICS = "-device virtio-gpu-pci"
# Virtio serial consoles
QB_SERIAL_OPT = "-device virtio-serial-pci -chardev null,id=virtcon -device virtconsole,chardev=virtcon"
QB_TCPSERIAL_OPT = "-device virtio-serial-pci -chardev socket,id=virtcon,port=@PORT@,host=127.0.0.1,nodelay=on -device virtconsole,chardev=virtcon"
# Virtio networking
QB_TAP_OPT = "-netdev tap,id=net0,ifname=@TAP@,script=no,downscript=no"


# u-boot
UBOOT_CONFIG ??= "rz-cmn"
UBOOT_CONFIG[rz-cmn] = "rz-cmn_defconfig"
